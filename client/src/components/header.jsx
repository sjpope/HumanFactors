import React from 'react';

function Header() {
    return (
        <header>
            <h1>TableSpotter</h1>
            <nav>
                <a>Sign Up</a>
                <a>Log In</a>
            </nav>
        </header>
    );
}

export default Header;
